const Router = require('express')
const router = new Router()

const catalogController = require('../controllers/catalogcontroller')

// router.get('/', catalogController.iflogged_in_response)
module.exports = router